@model UserManagement.ViewModels.LoginViewModel

<style>
    .btn:focus, .btn:active:focus, .btn-link.nav-link:focus, .form-control:focus, .form-check-input:focus {
        box-shadow: none;
    }
    .color-gray {
        color: #b6b6b6;
    }
</style>

@{
    Layout = "~/Views/Shared/_LayoutAccount.cshtml";
}

<div class="row">
    <div class="col-md-7">
        <div style="padding: 40px 120px;">
            <h2>THE APP</h2>
            <div class="p-5">
                <p class="m-0 color-gray">Start your journey</p>
                <h3 class="mb-5">Sign in to The app</h3>
                <form class="" asp-action="Login" method="post">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-control mb-3 d-flex justify-content-between align-content-center">
                        <div class="w-100">
                            @Html.LabelFor(x => x.Email, new { @class = "form-label m-0 color-gray" })
                            @Html.EditorFor(x => x.Email, new { htmlAttributes = new { @class = "form-control border-0 p-0 text-black", placeholder = "john@yahoo.com", type = "email" } })
                            @Html.ValidationMessageFor(x => x.Email, "", new { @class="text-danger" })
                        </div>
                        <i class='bx bxs-envelope p-1 d-flex align-items-center color-gray'></i>
                    </div>
                    <div class="form-control mb-3 d-flex justify-content-between align-content-center">
                        <div class="w-100">
                            @Html.LabelFor(x => x.Password, new { @class = "form-label m-0 color-gray" })
                            @Html.EditorFor(x => x.Password, new { htmlAttributes = new { @class = "form-control border-0 p-0 text-black", placeholder = "******", type = "password" } })
                            @Html.ValidationMessageFor(x => x.Password, "", new { @class="text-danger" })
                        </div>
                        <i class='bx bxs-lock-alt p-1 d-flex align-items-center color-gray'></i>
                    </div>
                    <div class="d-flex align-items-center mb-3">                        
                        @Html.CheckBoxFor(x => x.RememberMe, new { @class = "form-check-inline", id = "remember_me", type = "checkbox", style = "width: 20px; height:20px;" })
                        <label class="form-label m-0" for="remember_me">Remember me</label>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Sign in</button>
                </form>
            </div>
            <p class="color-gray">Don't have an account? <a asp-controller="Account" asp-action="SignUp">Sign up</a></p>
            <a class="btn btn-sm btn-secondary mt-2" asp-controller="Home" asp-action="Index">Back to home</a></>
        </div>
    </div>
    <div class="col-md-5">
        <img src="~/images/account-banner.jpg" class="w-100 h-100" />
    </div>
</div>